'use client'
import { useFormStatus } from "react-dom"


export const EmailUnameFormButton  = ({hasAnyInputChanged,action,id}:{hasAnyInputChanged:boolean,action?:any,id?:string})=>{
    const {pending}=useFormStatus()
    return(

    )
}